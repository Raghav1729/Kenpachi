import SwiftUI

struct Colors {
    // Disney Plus Brand Colors
    let primary = Color(red: 0.0, green: 0.4, blue: 0.8) // Disney Blue
    let primaryVariant = Color(red: 0.0, green: 0.3, blue: 0.7)
    
    // Secondary Colors
    let secondary = Color(red: 0.15, green: 0.15, blue: 0.2)
    let secondaryVariant = Color(red: 0.1, green: 0.1, blue: 0.15)
    
    // Background Colors
    let background = Color(red: 0.05, green: 0.05, blue: 0.08) // Very dark blue-black
    let surface = Color(red: 0.1, green: 0.1, blue: 0.15)
    let card = Color(red: 0.12, green: 0.12, blue: 0.18)
    
    // Text Colors
    let onPrimary = Color.white
    let onSecondary = Color.white
    let onBackground = Color.white
    let onSurface = Color.white
    let textPrimary = Color.white
    let textSecondary = Color(red: 0.7, green: 0.7, blue: 0.8)
    let textTertiary = Color(red: 0.5, green: 0.5, blue: 0.6)
    
    // Status Colors
    let success = Color(red: 0.2, green: 0.8, blue: 0.4)
    let warning = Color(red: 1.0, green: 0.6, blue: 0.0)
    let error = Color(red: 0.9, green: 0.2, blue: 0.2)
    let info = Color(red: 0.0, green: 0.4, blue: 0.8)
    
    // Accent Colors
    let accent = Color(red: 0.0, green: 0.4, blue: 0.8) // Disney Blue
    let highlight = Color(red: 1.0, green: 0.8, blue: 0.0) // Gold
    
    // Overlay Colors
    let overlay = Color.black.opacity(0.7)
    let scrim = Color.black.opacity(0.4)
    
    // Border Colors
    let border = Color(red: 0.2, green: 0.2, blue: 0.3)
    let divider = Color(red: 0.15, green: 0.15, blue: 0.2)
    
    // Button Colors
    let buttonPrimary = Color(red: 0.0, green: 0.4, blue: 0.8)
    let buttonSecondary = Color(red: 0.2, green: 0.2, blue: 0.3)
    let buttonDestructive = Color(red: 0.9, green: 0.2, blue: 0.2)
    
    // Gradient Colors
    let gradientStart = Color.black.opacity(0.9)
    let gradientEnd = Color.clear
    
    // Disney Brand Specific Colors
    let disneyBlue = Color(red: 0.0, green: 0.4, blue: 0.8)
    let pixarYellow = Color(red: 1.0, green: 0.8, blue: 0.0)
    let marvelRed = Color(red: 0.9, green: 0.1, blue: 0.1)
    let starWarsGold = Color(red: 1.0, green: 0.84, blue: 0.0)
    let natGeoYellow = Color(red: 1.0, green: 0.8, blue: 0.0)
}